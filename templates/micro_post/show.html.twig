{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}

    <h3>{{ post.title }}</h3>
    <div>{{ post.text }}</div>
    <div>{{ post.created|date('Y-m-d') }}</div>


{% endblock %}
